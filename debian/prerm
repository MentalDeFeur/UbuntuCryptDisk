#!/bin/bash
set -e

case "$1" in
    remove|purge)
        echo "Suppression d'UbuntuTPM2Disk..."
        echo "Note : les liaisons TPM2 existantes ne sont PAS supprimées automatiquement."
        echo "Pour supprimer une liaison : sudo ubuntu-tpm2disk unbind /dev/sdXY"
        ;;
esac

exit 0
